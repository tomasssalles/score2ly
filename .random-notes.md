# Random unsorted messy notes (Users: keep walking! There's nothing to see here)


## TODO (Phase 2)

- [ ] Refine this list with considerations from the section "Things to remember when fixing with an LLM" below (some items might be outdated though, and many items are about multi-round fixing, which has been postponed to phase 3).
- [ ] Add fix command. Checks that the new/update pipeline has been completed. Runs its own sequence of stages but continuing the numbering from new/update.
- [ ] Add LLM_PLAN stage with dummy implementation.
- [ ] Add interactive prompting for LLM parameters (model + API key).
- [ ] Add CLI args for LLM parameters (no --no-prompt option here because these are required parameters). Skip prompts for information provided via CLI args.
- [ ] Add config including default model and API key, functionality to read it. Use it in fix (CLI takes precedence over config, still prompt for missing information).
- [ ] Add config command to list, write and edit the config.
- [ ] Add llm_lite (or what was it called again?) dependency.
- [ ] Add a simple request in LLM_PLAN to test the code (especially against models from different providers).
- [ ] Get some API keys and test the simple request.
- [ ] Upgrade the simple request to one with a system prompt, a user prompt, a PNG attachment (should we also attach the .ly as a file, or just put it in the user prompt?), if supported by all providers, a strict schema for the response (like the pydantic models OpenAI supports) and a simple question about the image.
- [ ] Define + document exactly what the input and the response format/content of the per-system requests should be and plan + document how we're going to:
  - Generate new per-system .ly files from them
  - Generate a new full-score .ly file from them
- [ ] Define + document exactly what the input and the response format/content of the plan request should be and plan + document how we're going to:
  - Generate improved header information from them (but remember to respect fields already confirmed by the user)
  - Generate supporting input for the per-system requests from them
  - Generate supporting files for later transformations (new .ly files)
- [ ] Move prompt file to a prompts directory. Write system prompt and user prompt for LLM_PLAN stage.
- [ ] Implement + test full LLM_PLAN stage.
- [ ] Add LLM_FIX stage with dummy implementation.
- [ ] Write system prompt and user prompt for LLM_FIX stage (assume .ly snippet provided).
- [ ] Implement + test full LLM_FIX stage (assume .ly snippet provided).
- [ ] Add CLI arg for selecting specific systems to be fixed (the remaining ones being kept as they are). Maybe just a comma-separated list of system numbers. Maybe something fancier like "1-4;9;12-14;18;22". Test this.
- [ ] Handle (design + implement + test) case where a .ly snippet is missing. Maybe use a different system and user prompt pair. Maybe use the same prompts, but pass a valid .ly snippet with no measures (but then the prompts should perhaps mention that the snippet might be empty, which is always possible anyway).
- [ ] Add + implement + test FIXED_LY_ASSEMBLE stage (update transcription.ly link).
- [ ] Add + implement + test FIXED_LY_RENDER stage (share some code with current render stage) (update transcription.pdf link).
- [ ] Merge into main and release.


## Things to remember when fixing with an LLM

- Use LLMs in a separate "fix" command.
- If fix is run without the pipeline being finished, crash.
- If no API key provided, still try to fetch from cache before failing, that way "not passing an API key" becomes a safe way to re-run without risking costs or hitting API limits.
- We should include the system prompt in the hash. So the hash would use: system prompt + model + image bytes + xml/ly bytes, and we'd store the cache under ~/.score2ly/cache/<hash>.ly
- I'd like some human-readable way of finding the right cache entry. Maybe some JSONL file where we append dicts with the full path of the bundle, which system, which LLM model, a nanosecond timestamp and the cache entry path. We should also append new entries for cache-hits, i.e. when no new request is made, because the human looking for a cache entry might e.g. try looking for it based on the rough timestamp they remember running the pipeline.
- Gemini suggests I try Gemini 3.1 Pro and Claude 3.7 Sonnet (via OpenRouter/Puter). If the free tier API limits are too low, go for Gemini 2.5 Flash. Offer the user helpful tips on getting API keys with free-tier and which models to use.
- Use the LLM fixing stage in rounds, where each round uses the image plus the last output as the new input.
- After each round of fixing: merge and render.
- Maybe use a ~/.score2ly/config where the user can store a map of LLM models to API keys, as well as a default LLM model.
- Use a first request just with the PDF to draw up a plan: What instrument is being played? How many staves are needed in total? How many voices in each staff? Where and what are the key signatures? Where and what are the time signatures? What will be the names of the staves? What will be the names and roles of the voices? From the header elements that the user has not activelly accepted, given the extracted values, what would be the correct values? Pass this plan as part of the input in every subsequent request. This only has to be done once (no multiple rounds for this part).
- Instruct it to just output a dictionary mapping the names of the per-voice per-staff variables for the current system to their values.
- Try using Gemini 3.1 Pro in free-tier. Not the best per-minute and per-day limits, but should have good quality.
- Show it the full PDF (in grayscale) in every request, telling it "the system we're working on now is the third in page 5" for example. This is in addition to the cropped PNG of the individual system.
- Tell it that if something is unclear about the current system (e.g. a smudged note), it can use the PDF to look for similar chunks elsewhere in the score to help solve the ambiguity.
- Ask it to omit time and key signatures (also clef?) when they don't change (the snippets from audiveris+musicxml2ly will have these at the beginning of every system at least).
- Show it its own output for the previous 1-2 systems so it can work on consistency and continuity.
- Tell it to use only absolute notation.
- Tell it to pay attention to spacer rests.
- Tell it to fill every measure exactly, including the last one of the current system (because that's important for merging later).
- Whenever .ly files are generated (musicxml2ly, fix round 0, 1, 2, ...) I'd like to merge and render and update the "current best" links in the bundle. But I'm not sure how to handle human intervention (i.e. user manually edits some .ly file, maybe for a single system, maybe one of the merged ones). In that case there should be some updates (e.g. re-merging, re-rendering). But it gets very tricky very fast with these "dynamic stages" of multiple rounds of fixing.


## Early ideas (Phase 3)

- Multiple rounds of fixing with LLMs, using the results from one round as the starting point for the next.
- GUI
  - Manually editing stages and re-running in the GUI.
  - Inspecting, cropping, deskewing, CLAHE for PNGs in the GUI. (Always visualize the full source PNG, with the cropped one being defined by a bounding box in the view)
  - Show preview of rendering as the LLM works on the .ly snippets.
  - Edit .ly with side-by-side preview.
  - Drag and drop external MusicXML.
  - Run further rounds of LLM fix for selected measures/one selected measure at a time as needed.
- Handle multi-movement scores
- Package bundled with tool dependencies (controlled audiveris and lilypond versions)
